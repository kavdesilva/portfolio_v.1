const Portfolio = ({ portfolioRef }) => {
    return (
        <div className="scroll-to portfolio" ref={portfolioRef}>
            <h1>PORTFOLIO</h1>
            <section>
                <p>here goes the portfolio.</p>
            </section>
        </div>
    )
}

export default Portfolio